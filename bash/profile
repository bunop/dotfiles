# ~/.profile: executed by the command interpreter for login shells.
# This file is not read by bash(1), if ~/.bash_profile or ~/.bash_login
# exists.
# see /usr/share/doc/bash/examples/startup-files for examples.
# the files are located in the bash-doc package.

# the default umask is set in /etc/profile; for setting the umask
# for ssh logins, install and configure the libpam-umask package.
#umask 022

# if running bash
if [ -n "$BASH_VERSION" ]; then
    # include .bashrc if it exists
    if [ -f "$HOME/.bashrc" ]; then
	. "$HOME/.bashrc"
    fi
fi

# control write access to your terminal
mesg n

# set PATH so it includes user's private bin if it exists
if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/bin:$PATH"
fi

# to use per local libs
# local perl installation (activate only one times per shell)
[ $SHLVL -eq 1 ] && eval "$(perl -I${HOME}/Softwares/perl5/lib/perl5 -Mlocal::lib=${HOME}/Softwares/perl5)"

# to deactivate local::lib
# eval $(perl -Mlocal::lib=--deactivate-all)

# Using Pyenv
# export PYENV_ROOT="${HOME}/Softwares/pyenv"
# export PATH=$PYENV_ROOT/bin:$PATH
# eval "$(pyenv init -)"

# added by Anaconda2 4.0.0 installer
export PATH="${HOME}/Softwares/anaconda3/bin:$PATH"

# Add RVM to PATH for scripting. Make sure this is the last PATH variable change.
export PATH="$PATH:$HOME/Softwares/rvm/bin"

[[ -s "$HOME/Softwares/rvm/scripts/rvm" ]] && source "$HOME/Softwares/rvm/scripts/rvm" # Load RVM into a shell session *as a function*
